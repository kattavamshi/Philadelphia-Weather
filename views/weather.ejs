<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Weather channel</title>
<style>
  .text-center {
    text-align: center;
  }
</style>
</head>

<body>
  <h2 class="text-center">Weather conditions in <%= params %> is: </h2>
  <h3 class="text-center">
    <%  if( body.weather && body.weather.length > 0 ) { %>
      <%= body.weather[0].main %>
    <% } else { %>
      No data found!
    <% } %>
  </h3>
  <h4 class="text-center">
    <%  if( body.weather && body.weather.length > 0 ) { %>
      <%= body.weather[0].description %>
    <% } else { %>
    <% } %>
  </h4>
</body>

</html>
